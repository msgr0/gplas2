cat <<EOF >templates/final.yaml
samples:
  "${name}": "${input}"
reference:
  "${reference}": "${reference}"
species: "${species}"
threshold_prediction: "${threshold_prediction}"
number_iterations: "${number_iterations}"
classifier: "${classifier}"
filt_gplas: "${filt_gplas}"
name: "${name}"
EOF
